VITE_GOOGLE_DEVELOPER_KEY=REPLACE_WITH_GOOGLE_KEY
#Don't forget to limit this key to domain and APIs in Google Cloud Console
VITE_GOOGLE_CLIENT_ID=REPLACE_WITH_GOOGLE_CLIENT_ID

VITE_API_BASE_URL=http://localhost:8000

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_SUPABASE_ANON_KEY

# Server-only (do NOT add VITE_ prefix; keep these on backend / secret manager)
